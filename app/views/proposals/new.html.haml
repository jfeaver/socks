%h1 Which sock do you want to propose for a match with #{@sock.name}?

-# Reuses the same form structure as matches/new with a different submit action.
= form_with(model: @proposal, url: sock_proposals_path(sock_id: @sock.id)) do |f|
  %div
    = f.label :proposed_sock_id
    = f.select :proposed_sock_id, socks_options
  %div
    = f.submit "Send Proposal"
