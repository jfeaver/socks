- content_for :title, "Dashboard"

%div
  - @proposals.each do
    %p proposal waiting

%h1 Dashboard

%div{id: "socks"}
  - @socks.each do |sock|
    = render sock
    %p
      = link_to "Show this sock", sock

= link_to "New sock", new_sock_path